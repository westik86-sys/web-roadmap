# Design Team Roadmap

Минимальный статический прототип roadmap-страницы для дизайн-команды. В этой итерации реализованы только архитектура, форма состояния, тестовые данные и debug-рендер.

## Файлы

- `index.html` - семантический каркас страницы с секциями-заглушками
- `styles.css` - только структурные стили для базовой раскладки
- `data.js` - реалистичный sample dataset на несколько месяцев
- `script.js` - состояние приложения, утилиты, фильтрация и debug-рендер

## Запуск

Откройте `index.html` напрямую в браузере на macOS.

Сборка, package manager, локальный сервер и внешние зависимости не нужны.

## Что уже входит в текущий scope

- Базовая семантическая структура для header, filters, month navigation, roadmap, details и capacity
- Плоское состояние приложения с `selectedMonth`, `selectedTaskId` и активными фильтрами
- Утилиты для парсинга дат, подписей месяцев, week ranges, проверки пересечения задачи с месяцем и комбинированной фильтрации
- Черновой вывод задач для отладки и следующих UI-итераций
- Простой capacity summary по видимой нагрузке на assignee

## Что намеренно не сделано

- Финальный timeline UI
- Drag and drop
- Продвинутый capacity planning
- Сохранение состояния в URL или local storage
- Визуальная полировка и детальная адаптивность
- Редактирование задач и синхронизация с Jira

## Следующие итерации

1. Заменить debug-карточки задач на реальную time-based roadmap grid.
2. Добавить более явную группировку по stream, initiative или assignee.
3. Ввести лёгкие производные метрики для capacity и пересечений.
4. Добавить сохранение month, filters и selected task в URL.
